# Task: list available texture files

Add a function `list_texture_files()` to `utils.py` that returns a sorted list of the
file names (not full paths) inside the `assets/` directory tree, resolved relative to
`utils.py`'s own location so it works from any working directory.

Add a unit test in `tests/` covering it.

Follow the repository conventions in `AGENTS.md`.
